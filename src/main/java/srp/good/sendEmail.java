package srp.good;

public class sendEmail {

    public sendEmail(User user, String successfull_login) {
    }

    void sendEmail(User user, String msg) {
        // sending email to user
        String email = user.getEmail();
        // ....
    }
}
